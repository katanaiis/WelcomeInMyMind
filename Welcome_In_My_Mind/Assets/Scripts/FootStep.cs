using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FootStep : MonoBehaviour
{

    /*public GameObject fps;
    public AudioSource audio_In;
    public AudioSource audio_Out;


    // Start is called before the first frame update
    void Start()
    {
        fps = GetComponent<GameObject>();
    }

    // Update is called once per frame
    void Update()
    {
        if() //player est à l'intérieur du collider de room && est en mouvement
        {
            audio_In.Play();
        }
        else //player est à l'intérieur du collider de room && n'est pas en mouvement
        {
            audio_In.Stop()
        }
        if() //player est en dehors du collider de room && est en mouvement
        {
            audio_Out.Play()
        }
        else //player est en dehors du collider de room && n'est pas en mouvement 
        {
            audio_Out.Stop()
        }
    }*/
}
